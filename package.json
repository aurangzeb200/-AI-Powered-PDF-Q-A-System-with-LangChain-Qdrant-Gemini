{
  "name": "rag-demo-netlify",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@netlify/blobs": "^8.2.0",
    "@netlify/functions": "^4.2.4",
    "pdf2json": "^4.0.3"
  },
  "devDependencies": {
    "netlify-cli": "^23.4.1",
    "typescript": "^5.8.3"
  }
}
